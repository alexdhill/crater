% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_heatmap.r
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Plot a clustered heatmap of expression data}
\usage{
plot_heatmap(
  dds,
  genes,
  base_size = 8,
  samples = NA,
  gene_label = "gene_id",
  show_rownames = TRUE,
  show_colnames = FALSE,
  colors = c("red", "white", "blue"),
  scale = "logz",
  col_annotations = NA,
  row_annotations = NA,
  annotation_colors = NA,
  row_annotation_scale = 0.0625,
  col_annotation_scale = 0.0625,
  row_dendro_scale = 0.125,
  col_dendro_scale = 0.125,
  labeller = NA,
  filename = NA,
  assemble = TRUE
)
}
\arguments{
\item{dds}{A DESeqDataSet object from DESeqDataSetFromCreate}

\item{genes}{A character vector of gene IDs or a logical vector indicating which genes to plot}

\item{base_size}{Base font size for the plot \link{default=8}}

\item{samples}{A character or logical vector of samples to include \link{default=NA, all samples}}

\item{gene_label}{Column in rowData to use as row labels \link{default="gene_id"}}

\item{show_rownames}{Whether to show row (gene) labels \link{default=TRUE}}

\item{show_colnames}{Whether to show column (sample) labels \link{default=FALSE}}

\item{colors}{A length-3 vector of colors for the gradient (low, mid, high) \link{default=c("red","white","blue")}}

\item{scale}{Scaling method: "log" (log10+1), "logz" (log10+1 then z-score), or "z" (z-score) \link{default="logz"}}

\item{col_annotations}{A data.frame of column annotations, rownames must match sample names \link{default=NA}}

\item{row_annotations}{A data.frame of row annotations, rownames must match gene IDs \link{default=NA}}

\item{annotation_colors}{A named list of color vectors for each annotation column \link{default=NA}}

\item{row_annotation_scale}{Relative width of row annotation tiles \link{default=0.0625}}

\item{col_annotation_scale}{Relative height of column annotation tiles \link{default=0.0625}}

\item{row_dendro_scale}{Relative width of the row dendrogram \link{default=0.125}}

\item{col_dendro_scale}{Relative height of the column dendrogram \link{default=0.125}}

\item{labeller}{A named character vector mapping gene IDs to display labels \link{default=NA}}

\item{filename}{Path to save the plot; if NA the plot is returned only \link{default=NA}}

\item{assemble}{If TRUE (default), return the assembled patchwork. If FALSE, return a list
with \verb{$plots}, \verb{$design}, \verb{$widths}, and \verb{$heights} so the panels can be integrated into
an outer patchwork at the same level (required for \code{guides = 'collect'} to work across
the boundary).}
}
\value{
A patchwork plot object, or a list of components when \code{assemble = FALSE}
}
\description{
Plot a clustered heatmap of expression data
}
